"""Cron task domain owners.

The package keeps profile-scoped cron persistence, manual execution, output
history, and completion projection behind small interfaces.  HTTP adaptation
lives in :mod:`api.routes_parts.cron`.
"""
